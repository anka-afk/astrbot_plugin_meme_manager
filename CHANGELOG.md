# 更新日志

本文件记录 AstrBot 表情包管理器的主要版本更新，最新版本排在最前。

## v5.0.6

- 预览图片排队加载，默认并发 2 张，可在 AstrBot 插件配置和设置中心调整为 1–8 张；保存后刷新页面生效，并发限制按每个页面计算。
- 增加浏览器私有缓存和文件版本校验，刷新或重开页面时复用未变化的图片；重新加载图库后识别文件新版本，减少重复下载。
- 管理页、审核预览与语义页复用加载队列，支持请求去重、主动预览优先和过时等待任务清理，改善低带宽体验（[#134](https://github.com/anka-afk/astrbot_plugin_meme_manager/issues/134)）。

## v5.0.5

- 自动收集默认启用人工审核，支持分类排序、批量处理及相似图片预览；每包待审核容量可配置，默认 200 张。
- 优化去重与识图结果复用，语义索引仍需手动生成。
- 修复空表情包提示注入和回复中残留表情标签的问题。
- 支持兰空图床开源版 2.x。

## v5.0.3

- 打开语义页面仅读取已有状态；图片扫描和旧数据迁移延后至手动启动任务，并在后台执行，减少卡顿。
- 保留已有描述复用及检索时的图片内容校验，避免使用被替换图片的旧记录。

## v5.0.2

- 重制 R2、WebDAV 和 StarDots 图床同步，支持内容差异与冲突识别、双向覆盖、任务进度及取消；修复目录、分页和文件标识等兼容问题。
- 下载校验后再替换文件；覆盖同步在传输和复核成功后才清理多余图片，失败或取消会阻止后续清理。
- **升级提醒**：旧版上传记录不足以确认同名图片内容，首次比较可能显示“待确认”。普通同步保留冲突；覆盖会删除目标端多出的图片，请先检查差异。已有图片和旧记录不会自动搬迁或删除。

## v5.0.1

- 新增每条回复的表情数量硬上限：负数不限量、`0` 不发送、正数保留前 N 个；另提供可选的频率与数量提示模板。
- 精简默认提示词，明确内部标签、ID 和描述的使用边界。
- **升级提醒**：按需在“设置中心 → 提示词模板”恢复默认模板并保存；已有自定义内容请先备份。

## v5.0.0

- 重构 WebUI 和可搜索的分类设置中心，重写默认提示词及完整、流式回复的表情解析。
- **破坏性更新**：移除旧版平铺配置、配置别名及旧表情目录自动迁移。升级前请备份，升级后检查并补齐设置；已有表情包文件保留。
- 整理后端和测试目录，分离用户文档与开发指南。

## v4.15.5

- 修复 SiliconFlow 等视觉模型拒绝工具调用时的自动降级，复用降级缓存，避免重复尝试不受支持的请求格式（[#123](https://github.com/anka-afk/astrbot_plugin_meme_manager/issues/123)）。

## v4.15.4

- 修复 Gemini 因分类建议工具参数包含空字符串枚举而报错的问题，保留分类白名单校验。

## v4.15.3

- NapCat 自动收集预先过滤非表情消息；完善资源包下载的真实进度、超时、加速源回退、取消及页面重连。
- 语义化优先复用已有描述，仅补充缺失或失败项；强制重新生成时才覆盖已有描述。

## v4.14

- 修复同步与资源包路径越界、中文上传文件名、StarDots 命名及部分同步失败仍报告成功的问题。
- 分类操作增加失败回滚，管理命令接入任务锁；覆盖同步失败时停止清理，上传与下载顺序执行。

## v4.13

- 新增自动收集表情包（BETA）。
- 资源广场支持后台安装与真实下载进度，分块下载并限制压缩包大小。

## v4.12

- 加强路径校验，修复管理页 DOM XSS 和运行时切包仍操作旧包的问题。
- 将耗时的图片、网络及语义处理移出事件循环，完善取消时的任务锁管理。

## v4.1

- 新增语义向量检索，情感辅助选图支持会话上下文和人格注入。
- 社区安装支持 GitHub 加速与原生源回退，页面图标改为本地资源。

## v4.0

- 新增资源广场、多表情包管理、会话与人格选包、全量备份，以及按当前包执行的云同步。
- 发布表情包协议与创作指南，提供其他插件可调用的表情发送接口。
- **破坏性更新**：仓库根目录 `memes/` 不再是默认资源来源，首次启动为空表情包；配置结构调整，升级后请检查设置。

## v3.21

- 管理界面迁移至 AstrBot WebUI；数据使用 `data/plugin_data/meme_manager`，兼容迁移旧版 `data/memes_data`。
- 完善批量管理、上传去重、拖拽、预览、移动端布局与同步进度，危险操作增加确认。
- 首次启动不再自动导入默认表情包；“恢复默认表情包”改为从官方仓库安装。

## v3.1x

- 兼容 AstrBot 4.5.0+，完善标签匹配、重复检测、WebP 和上传支持。
- 新增 Cloudflare R2、图文混合回复及概率控制，改进同步统计与消息链处理。

## v3.0x

- 修复消息类型兼容问题，移除 `imghdr` 依赖以支持新版 Python。

## v3.0

- 重构代码架构，新增 WebUI、图床同步并优化表情识别。

## v2.2

- 扩充表情包，修复 TTS 兼容性。

## v2.1

- 优化发送逻辑，文本与表情分开发送。

## v2.0

- 支持网络图片上传，优化上传流程。

## v1.x

- 首次发布，提供基础表情管理和多图上传。
